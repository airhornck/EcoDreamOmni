# ── Web Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# ── Database ──
sqlalchemy>=2.0.0
asyncpg>=0.30.0
alembic>=1.13.0

# ── Validation & Settings ──
pydantic>=2.9.0
pydantic-settings>=2.5.0
email-validator>=2.2.0

# ── Auth & Security ──
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
python-multipart>=0.0.12

# ── Task Queue ──
celery>=5.4.0
redis>=5.1.0

# ── HTTP Client ──
httpx>=0.27.0

# ── ML & Data ──
scikit-learn>=1.5.0
numpy>=2.0.0
pandas>=2.2.0
jieba>=0.42.0

# ── Observability (V2.4+) ──
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0
opentelemetry-instrumentation-fastapi>=0.49b0
opentelemetry-instrumentation-celery>=0.49b0
opentelemetry-instrumentation-redis>=0.49b0
opentelemetry-exporter-otlp>=1.28.0
prometheus-client>=0.21.0

# ── Resilience (V2.5+) ──
pybreaker>=1.0.0
stamina>=24.0.0

# ── Workflow & Scheduling (V2.5+) ──
croniter>=3.0.0
transitions>=0.9.0

# ── Prompt Management (V2.6+) ──
langfuse>=2.0.0

# ── Evaluation (V2.6+) ──
deepeval>=1.0.0

# ── Vector DB (V2.7.1+) ──
pgvector>=0.2.0

# ── PDF Generation (V2.7.1+) ──
weasyprint>=59.0

# ── Image Processing (V2.7.1+) ──
pillow>=10.0.0

# ── Logging ──
structlog>=24.0.0

# ── Testing ──
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
bcrypt==3.2.2

# ── SOCKS5 Proxy Support ──
PySocks>=1.7.1

# ── XHS Platform SDK ──
xhs>=0.2.13
