2026年企业级大厂（如 Linear、Framer、Retool、Dify）最主流的解法正是你提到的第一种——“聚焦态折叠卡片（Focus-State Expandable Node）”：

核心设计逻辑：未激活时，卡片仅作为“摘要/只读节点”（只显示关键配置和流向状态），极其紧凑；一旦点击某个节点，该节点会通过优雅的微动效向外或向下平展放大（Expand），露出全量复杂表单（如策略组合、变量推导等）。配置保存后，再次收起为精简卡片。

这种做法既保证了全局“流（Flow）”的清晰，又兼顾了局部“深度配置”的体验。

我为你重新调整并升级了 HTML 互动预览。在这个版本中，我为你实现了动态的交互效果：

点击任意卡片：它会平滑地放大（宽度增加、高度展露、阴影加深、边框高亮），并显示出你提到的全部复杂字段（如 Step 2 的变量推导、Step 4 的执行方式）。

点击其他卡片或保存：原卡片会自动缩小回“只读摘要状态”。