# 开发变更总结 — 2026-05-28

> **需求概述**: 用户测试反馈专项评审 v1.1 — 修复 8 项问题 + 实施 3 项新增需求  
> **PRD 章节**: §Dashboard / §PlatformRule / §Publisher / §DataAnalyst / 新增体验需求

---

## 修改文件清单

### 前端（Frontend）

| 文件 | 类型 | 说明 |
|------|------|------|
| `apps/frontend/src/pages/DashboardPage.tsx` | 修改 | 隐藏 3 个快捷入口；修复 quickActions 类型 |
| `apps/frontend/src/components/review/TopicReportPanel.tsx` | 修改 | 防御式编程：`report.topics?.find` 防空指针崩溃 |
| `apps/frontend/src/pages/DataAnalystPage.tsx` | 修改 | 数值字段防御式编程（`?? 0`）防止 `undefined` 崩溃 |
| `apps/frontend/src/pages/ReviewPublishDetailPage.tsx` | 修改 | 移除发布确认中定时/循环 UI；简化 confirmPublish 为 immediate-only；CoverPickerModal 调用支持 ratio |
| `apps/frontend/src/components/review/PlatformPreviewCard.tsx` | 修改 | 标题编辑引导增强（常驻编辑图标 + hover 提示）；平台规则校验（maxLength + 字数提示） |
| `apps/frontend/src/components/review/CoverPickerModal.tsx` | 修改 | 新增 6 种封面比例选择（1:1, 3:4, 4:3, 9:16, 16:9, 3:1）；onSelect 接口扩展为 `(url, ratio?)` |
| `apps/frontend/src/components/review/MarkdownBody.tsx` | 重写 | 替换 textarea 为 TipTap 富文本编辑器（加粗/斜体/标题/列表/引用/代码） |
| `apps/frontend/src/stores/reviewPublishStore.ts` | 修改 | `GeneratedContent` 接口新增 `cover_image_ratio?: string` |
| `apps/frontend/package.json` | 修改 | 新增依赖：`@tiptap/react`, `@tiptap/starter-kit`, `@tiptap/extension-placeholder` |

### 后端（Backend）

| 文件 | 类型 | 说明 |
|------|------|------|
| `apps/backend/src/main.py` | 修改 | FastAPI Swagger 路径调整为 `/api/docs`（适配 nginx 代理） |
| `apps/backend/src/services/data_analyst_service.py` | 修改 | `get_dashboard_summary` 返回字段与前端 `DashboardData` 对齐（totalPublished/avgCoverage/avgMape/avgLikes 等） |
| `apps/backend/src/api/data_analyst.py` | 修改 | `DashboardOut` 模型字段与前端对齐；`get_dashboard` 映射新字段 |

### 基础设施（Infrastructure）

| 文件 | 类型 | 说明 |
|------|------|------|
| `scripts/generate_api_dictionary.py` | 新增 | 后端 API 路由扫描脚本，自动生成数据词典 02-后端API路由层.md |
| `apps/backend/src/data/platforms/*.yml` | 新增 | 从 `D:\project\lumina\data\platforms` 同步平台规则 YAML（bilibili_v2024.yml, douyin_v2024.yml, wechat_official.yml, xiaohongshu_v2024.yml） |

### 文档（Docs）

| 文件 | 类型 | 说明 |
|------|------|------|
| `docs/问题分析报告_用户测试反馈专项评审_v1.md` | 新增 | 问题分析报告 v1.1（936 行） |
| `docs/变更记录/2026-05-28/强制检查清单_2026-05-28.md` | 新增 | 六章强制检查清单 |
| `docs/数据词典/02-后端API路由层.md` | 重写 | 由脚本重新生成，52 个路由文件、125 个端点 |

---

## 调用关系变更

```
前端: PlatformPreviewCard → onChange({ title }) → handleContentPatch → updateContent → PUT /api/review-publish-center/conclusions/{id}/content
前端: CoverPickerModal → onSelect(url, ratio) → handleContentPatch({ cover_image_url, cover_image_ratio })
前端: MarkdownBody → TipTap Editor → onChange(html) → updateContent
后端: api/data_analyst.py → get_dashboard_summary → 返回 DashboardOut(totalPublished/avgCoverage/avgMape/avgLikes)
后端: api/review_publish.py → confirm_publish → 仅支持 immediate 模式
```

---

## 测试验证

### 前端

```bash
cd apps/frontend && npm run build
# 结果: tsc -b && vite build ✓ built in 730ms
# 错误: 0
```

### 后端

```bash
# FastAPI Swagger 已启用: /api/docs
# 数据词典脚本验证: 52 个路由文件，125 个端点
```

---

## 文档更新

- [x] `docs/数据词典/02-后端API路由层.md` — 脚本重新生成
- [x] `docs/变更记录/2026-05-28/开发变更总结_2026-05-28.md`
- [x] `docs/变更记录/2026-05-28/强制检查清单_2026-05-28.md`
- [x] `docs/问题分析报告_用户测试反馈专项评审_v1.md`
- [ ] `docs/PRD偏差报告.md` — 待更新
- [ ] `docs/文档总纲.md` — 待更新接口管理分类

---

## 风险与后续

| 风险项 | 等级 | 说明 |
|--------|------|------|
| TipTap 包体积 | 🟡 中 | 主 JS 包从 810KB 增至 1183KB（+370KB gzip），建议在 W16 评估代码分割 |
| 富文本 HTML 存储 | 🟡 中 | 后端 `body` 字段现存储 HTML 而非 Markdown，旧数据预览兼容逻辑已添加（`isHtmlContent` 检测） |
| Docker 构建 | 🟢 低 | 前端本地构建已通过，Docker 守护进程未运行未验证，但无新增编译错误 |
| 封面比例后端处理 | 🟡 中 | `cover_image_ratio` 仅前端存储，后端 ImageForge 尚未接入（排入 W16） |
| 工作流平台规则注入 | 🔴 高 | 已排入 W15 专项迭代，当前 content_generator 仍使用硬编码规则 |

### 待办

1. **W15**: 工作流引擎注入平台规则（YAML → execution context → content-forge prompt）
2. **W16**: ImageForge 接入封面比例裁剪
3. **W19-W20**: AccountPool 状态机 + 自动熔断 + 每日配额
