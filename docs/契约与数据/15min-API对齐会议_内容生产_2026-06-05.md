# 15min API 对齐会议 — 内容生产 `/generate`

> **日期**: 2026-06-05  
> **参会人**: 前端技术 + 后端技术  
> **会议目标**: 冻结内容生产页面 API 契约，确认前后端实现细节  
> **前置条件**: Step 1 HTML 预览 + 后端契约草案已通过四方审核

---

## 一、API 清单确认（8 已有 + 2 新增 + 4 通用）

### 1.1 已有 API 扩展（无需新增端点，仅扩展响应字段）

| # | 方法 | 路径 | 前端解包路径 | 后端实现状态 |
|---|------|------|-------------|-------------|
| 1 | GET | `/api/task-hub/tasks` | `data.data.copilot_summary` | Sprint 1 |
| 2 | GET | `/api/task-hub/tasks/{id}` | `data.data.copilot_context` | Sprint 1 |
| 3 | POST | `/api/task-hub/tasks` | `data.data` + `data.copilot_followup` | Sprint 1 |
| 4 | GET | `/api/agents` | `data.data` | 已有 ✅ |
| 5 | GET | `/api/agents/recommend` | `data.data` | 已有 ✅ |
| 6 | GET | `/api/account-pool` | `data.data` | 已有 ✅ |
| 7 | GET | `/api/personas` | `data.data` | 已有 ✅ |
| 8 | GET | `/api/platform-schemas` | `data.data` | 已有 ✅ |

**对齐确认**: ✅ 前端统一从 `data.data` 解包，`copilot_summary` / `copilot_context` / `copilot_followup` 在 `data` 根层级

### 1.2 新增 API（内容生产专属）

| # | 方法 | 路径 | 用途 | 后端实现状态 |
|---|------|------|------|-------------|
| 9 | POST | `/api/ai/copilot/regenerate-content` | 重新生成内容 | Sprint 2 |
| 10 | POST | `/api/ai/copilot/save-and-submit` | 保存并提交审核 | Sprint 2 |

**对齐确认**: ✅ 统一走 `/api/ai/copilot/*` 网关，前端 `apiClient` 自动补 `/api` 前缀

### 1.3 通用 Copilot 网关（所有页面共享）

| # | 方法 | 路径 | 用途 | 后端实现状态 |
|---|------|------|------|-------------|
| 11 | POST | `/api/ai/copilot/context` | 上下文上报 | Sprint 1（stub）|
| 12 | GET | `/api/ai/copilot/action-cards` | 获取推荐 Cards | Sprint 1（stub）|
| 13 | POST | `/api/ai/copilot/execute` | 通用 Action 执行 | Sprint 1（stub）|
| 14 | WS | `/ws/copilot` | 实时通道 | Sprint 1（stub）|

**对齐确认**: ✅ 通用网关 Sprint 1 骨架已完成，内容生产 Sprint 2 接入业务逻辑

---

## 二、关键字段对齐

### 2.1 `copilot_summary`（看板页）

```typescript
// 前端 TypeScript 类型
interface CopilotSummary {
  kanban_stats: { draft: number; reviewing: number; approved: number; published: number }
  recommended_focus: 'draft' | 'reviewing' | 'approved'
  ai_insight: string
  suggested_actions: Array<{
    type: 'create_task' | 'batch_generate' | 'batch_publish'
    label: string
    reason: string
  }>
}
```

**对齐确认**: ✅ 前后端字段名、类型、枚举值一致

### 2.2 `copilot_context`（编辑器页）

```typescript
interface CopilotContext {
  editor_suggestions: Array<{
    type: 'title_optimization' | 'tag_expansion' | 'body_enhancement'
    confidence: number  // 0.00~1.00
    reason: string
    suggested_title?: string
    suggested_tags?: string[]
  }>
  save_status: 'saved' | 'unsaved_changes' | 'saving'
  recommended_next: 'save_draft' | 'submit_review' | 'regenerate'
  generation_progress: { layer: string; progress: number } | null
}
```

**对齐确认**: ✅ 前后端字段名、类型、枚举值一致

### 2.3 `copilot_followup`（通用）

```typescript
interface CopilotFollowup {
  message: string
  suggested_cards: Array<{
    type: 'action' | 'suggestion' | 'confirm' | 'info' | 'decision' | 'navigation'
    title: string
    description?: string
    priority?: number
    inputs?: Array<{ name: string; type: string; label: string; options?: string[] }>
    actions: Array<{
      id: string
      label: string
      variant: 'primary' | 'secondary' | 'ghost'
      api?: { method: string; endpoint: string; payload: Record<string, unknown> }
    }>
    diff?: { before: string; after: string }
  }>
}
```

**对齐确认**: ✅ 与审核发布页面共用同一 `CopilotFollowup` 类型定义

---

## 三、WebSocket 事件对齐

### 3.1 内容生产专属事件

| 事件名 | Payload 结构 | 前端处理 |
|--------|-------------|---------|
| `content.generation.progress` | `{task_id, progress: 0.6, layer: "Layer 5: Persona"}` | 更新六层 Prompt 进度条 |
| `content.regeneration.complete` | `{task_id, job_id, new_content: {title, body, hashtags}}` | 刷新编辑器内容，推送完成 Card |
| `content.save_status` | `{task_id, status: "saved", version: 3}` | 更新保存状态指示器 |

**对齐确认**: ✅ 事件名使用 `content.` 前缀，与审核发布 `review.` 前缀区分

### 3.2 通用 Copilot 事件

| 事件名 | 说明 |
|--------|------|
| `context.update` | 客户端上报上下文 |
| `card.push` | 服务端推送 Action Card |
| `card.execute` | 客户端请求执行 |
| `card.result` | 服务端返回结果 |
| `card.progress` | 长时间任务进度 |
| `insight.push` | AI 洞察推送 |
| `workspace.sync` | 工作区数据同步 |

**对齐确认**: ✅ 复用审核发布已定义的通用事件

---

## 四、API 前缀与路由对齐

| 前端调用 | 后端实际路由 | Nginx/ALB 代理 | 状态 |
|---------|-------------|----------------|------|
| `GET /api/task-hub/tasks` | `GET /task-hub/tasks` | 自动补 `/api` | ✅ 一致 |
| `POST /api/ai/copilot/regenerate-content` | `POST /ai/copilot/regenerate-content` | 自动补 `/api` | ✅ 一致 |
| `POST /api/ai/copilot/save-and-submit` | `POST /ai/copilot/save-and-submit` | 自动补 `/api` | ✅ 一致 |
| `WS /ws/copilot` | `WS /ws/copilot` | 直接代理 | ✅ 一致 |

**历史问题修复**: 审核发布联调中发现 `/api/ai/generate-cover` 与后端 `/ai/copilot/generate-cover` 不一致，本次统一约定：**所有 Copilot 相关 API 统一走 `/ai/copilot/*` 路径，前端调用时加 `/api` 前缀。**

---

## 五、错误码对齐

| 错误码 | HTTP 状态 | 前端处理 | 后端实现 |
|--------|----------|---------|---------|
| `TASK_ALREADY_SUBMITTED` | 409 | 刷新状态，提示"该任务已提交审核" | Sprint 2 |
| `TASK_NOT_FOUND` | 404 | 跳转 404 或看板页 | 已有 ✅ |
| `AGENT_NOT_SUPPORTED` | 400 | 提示"当前 Agent 不支持该风格" | Sprint 2 |
| `GENERATION_IN_PROGRESS` | 409 | 提示"已有生成任务进行中"，展示进度 | Sprint 2 |
| `COPILOT_CONTEXT_EXPIRED` | 400 | 自动刷新后重试 | Sprint 1 |

**对齐确认**: ✅ 错误码与 `01-API接口契约.md` §2.2 全局字典一致

---

## 六、Mock 策略对齐

在通用 Copilot 网关 Sprint 1 完成前，前端使用以下 Mock：

```typescript
// src/lib/mockCopilot.ts
export const mockCopilotAPI = {
  async getActionCards(page: string, contextId: string) {
    // 返回静态 Action Cards（按页面区分）
    return page === '/generate' ? generatePageCards : editorPageCards
  },
  async executeAction(actionId: string, payload: unknown) {
    await delay(800)
    return {
      success: true,
      data: generateMockResult(actionId, payload),
      copilot_followup: generateMockFollowup(actionId)
    }
  }
}
```

**对齐确认**: ✅ 后端 Sprint 1 完成后，前端切换 `USE_MOCK = false`，逐 API 验证

---

## 七、性能约定

| 指标 | 目标 | 监控方式 |
|------|------|---------|
| `GET /api/task-hub/tasks` | ≤ 150ms | 后端 Prometheus + 前端 RUM |
| `GET /api/task-hub/tasks/{id}` | ≤ 100ms | 后端 Prometheus + 前端 RUM |
| `POST /api/task-hub/tasks` | ≤ 200ms | 后端 Prometheus |
| `POST /api/ai/copilot/regenerate-content` | ≤ 100ms（提交）| 后端 Prometheus |
| `POST /api/ai/copilot/save-and-submit` | ≤ 200ms | 后端 Prometheus |
| 内容生成完成（异步） | ≤ 15s（P95）| 后端任务监控 |
| WebSocket 推送延迟 | ≤ 100ms | 后端 Prometheus |

**对齐确认**: ✅ 性能目标写入 `01-API接口契约.md` §八

---

## 八、会议结论

| 检查项 | 结果 |
|--------|------|
| API 清单（14 个） | ✅ 前后端一致 |
| 字段类型（copilot_summary / copilot_context / copilot_followup） | ✅ 前后端一致 |
| WebSocket 事件（10 个） | ✅ 前后端一致 |
| API 前缀 | ✅ 统一 `/ai/copilot/*` |
| 错误码（5 个内容生产专属） | ✅ 与全局字典一致 |
| Mock 策略 | ✅ 前端 stub，后端 Sprint 1/2 逐接口替换 |
| 性能目标 | ✅ 已约定 |

**契约冻结状态**: 🔒 **已冻结**（2026-06-05）

**变更控制**: 冻结后任何 API 变更需经前后端技术双方确认，并更新本文档 + `01-API接口契约.md`

---

*会议记录人: Kimi Code CLI*  
*冻结日期: 2026-06-05*  
*下一步: Step 3 前后端并行开发*
