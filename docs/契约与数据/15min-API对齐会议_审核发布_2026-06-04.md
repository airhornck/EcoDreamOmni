# 15min API 对齐会议 — 审核发布 `/review`

> **日期**: 2026-06-04
> **参会人**: 产品 + 设计 + 前端技术 + 后端技术
> **前置条件**: Step 1 HTML 预览已通过四方审核
> **目标**: 冻结契约，前后端并行开发无歧义

---

## 会议议程（严格计时 15min）

### 0. 开场（1min）
- 主持人确认四方代表到齐
- 明确规则：本次会议只讨论「是否达成一致」，不讨论「是否需要这个功能」
- Step 1 成果确认：`demo/page-preview/review.html` v3 已通过审核

---

### 1. 路由对齐（1min）

| 项 | 前端 | 后端 | 状态 |
|---|---|---|---|
| 列表页 | `/review` | `/api/review-publish-center/conclusions` | ✅ 已对齐 |
| 详情页 | `/review/:taskId` | `/api/review-publish-center/conclusions/{id}` | ✅ 已对齐 |
| 旧路由 | `/review-publish-center` → `/review` | 保留兼容 1 Sprint | ✅ 已对齐 |

**确认问题**: 后端是否保留 `/review-publish-center/*` 1 个 Sprint 的 301 重定向？
- [x] 是 — 前端无需改旧路由调用
- [ ] 否 — 前端需要同时改 API path

---

### 2. API 清单逐一确认（6min）

#### 2.1 现有 API（无变更，仅确认）

| # | Method | Endpoint | 变更点 | 确认 |
|---|--------|----------|--------|------|
| 1 | GET | `/api/review-publish-center/conclusions` | 响应新增 `copilot_summary` | ✅ |
| 2 | GET | `/api/review-publish-center/conclusions/{id}` | 响应新增 `copilot_context` + `available_copilot_cards` | ✅ |
| 3 | POST | `/api/human-in-the-loop/tasks/{id}/{decision}` | 请求新增 `copilot_suggested` + `copilot_card_id`；响应新增 `copilot_followup` | ✅ |
| 4 | PUT | `/api/review-publish-center/conclusions/{id}/content` | 无变更 | ✅ |
| 5 | POST | `/api/review-publish-center/conclusions/{id}/confirm-publish` | 无变更 | ✅ |
| 6 | POST | `/api/review-publish-center/conclusions/{id}/regenerate` | 无变更 | ✅ |

#### 2.2 新增 API（需重点确认）

| # | Method | Endpoint | 责任人 | 确认 |
|---|--------|----------|--------|------|
| 7 | POST | `/api/ai/generate-cover` | 后端 Sprint 2 | ✅ |
| 8 | GET | `/api/ai/generate-cover/{job_id}` | 后端 Sprint 2 | ✅ |
| 9 | POST | `/api/ai/copilot/context` | 后端 Sprint 1 | ✅ |
| 10 | GET | `/api/ai/copilot/action-cards` | 后端 Sprint 1 | ✅ |
| 11 | POST | `/api/ai/copilot/execute` | 后端 Sprint 1 | ✅ |

#### 2.3 关键确认项（逐条表决）

- [x] **A. `copilot_followup` 字段**: 所有业务 API 响应可选携带，前端不解包时不报错
- [x] **B. 审核决策响应**: approve → 自动携带 `copilot_followup` 推送「发布确认」Card
- [x] **C. 封面生成**: 异步任务（ACCEPTED 200ms → WebSocket 推送），前端先轮询后切 WebSocket
- [x] **D. Action Cards 获取**: 后端根据 `page` + `task_id` 动态组装，非静态配置
- [x] **E. 通用执行网关**: `/api/ai/copilot/execute` 透传到底层 API，响应格式同底层 API

---

### 3. 数据模型确认（3min）

| 表/字段 | 变更 | 迁移脚本责任人 | 确认 |
|---------|------|---------------|------|
| `ai_cover_generation_jobs` | 新增 | 后端 | ✅ |
| `copilot_context_sessions` | 新增（+ `expires_at` TTL 30min） | 后端 | ✅ |
| `copilot_action_logs` | 新增 | 后端 | ✅ |
| `review_conclusions.copilot_recommended_action` | 扩展 | 后端 | ✅ |
| `review_conclusions.copilot_confidence` | 扩展 | 后端 | ✅ |
| `review_conclusions.copilot_reasoning` | 扩展 | 后端 | ✅ |
| `review_conclusions.copilot_suggested_improvements` | 扩展 | 后端 | ✅ |

**确认问题**:
- [x] `copilot_context_sessions.expires_at` 默认 `NOW() + 30min`，过期自动清理方式：查询时清理 + Cron 每日清理
- [x] `copilot_action_logs` 保留 90 天，清理策略：Cron 每日清理过期记录

---

### 4. WebSocket 事件确认（2min）

| 事件名 | 方向 | 优先级 | Sprint | 确认 |
|--------|------|--------|--------|------|
| `cover.generation.progress` | S→C | P0 | 2 | ☐ |
| `cover.generation.completed` | S→C | P0 | 2 | ☐ |
| `review.decision.completed` | S→C | P1 | 2 | ☐ |
| `copilot.card.push` | S→C | P1 | 1 | ☐ |
| `workspace.sync` | S→C | P2 | 3 | ☐ |

**确认问题**:
- [ ] WebSocket 通道使用统一 `/ws/copilot` 还是审核发布独立通道？
  - **推荐**: 统一 `/ws/copilot`（已在全局文档定义）

---

### 5. Mock 策略与并行开发（1min）

| 项 | 前端 | 后端 | 确认 |
|---|---|---|---|
| Sprint 1 前端是否阻塞？ | 否，使用 `lib/mockCopilot.ts` | 后端提供 Postman Collection | ☐ |
| Sprint 2 联调窗口 | 后端 API 就绪后 2 天 | 提供 Swagger 文档 | ☐ |
| 后端缺口记录 | `docs/后端需求/后端需求补充_审核发布_Copilot-Driven_2026-06-04.md` | — | ✅ 已存在 |

---

### 6. 冻结签字（1min）

四方代表确认以下文档为并行开发真源：

1. `docs/契约与数据/01-API接口契约.md` §十一
2. `docs/数据词典_v4.0/01-数据库模型层.md` §五
3. `docs/数据词典_v4.0/04-前端Store与路由.md`（/review 路由 + Store 映射）
4. `docs/后端需求/后端需求补充_审核发布_Copilot-Driven_2026-06-04.md`
5. `docs/后端需求/后端需求补充_全局_Copilot-Driven_2026-06-04.md`

**签字**:
- [x] 产品代表: __产品负责人__
- [x] 设计代表: __设计负责人__
- [x] 前端技术: __前端架构师__
- [x] 后端技术: __后端架构师__

**冻结后变更流程**: 任何契约变更需四方重新签字，并更新变更记录。

---

## 会议输出

- [x] 本会议纪要通过邮件/文档同步全员
- [x] 前端更新 `04-前端Store与路由.md` 冻结标记
- [x] 后端更新 `01-API接口契约.md` 冻结标记
- [x] 后端开始 Sprint 1 通用网关开发
- [x] 前端开始 Step 3 React 实现（基于 mock）

---

*会议文档版本: v1.0*
*制定日期: 2026-06-04*
*状态: **已冻结** — 2026-06-05 四方联合审核通过*
