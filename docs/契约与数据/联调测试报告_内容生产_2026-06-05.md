# 联调测试报告 — 内容生产 `/generate`

> **日期**: 2026-06-05  
> **前端版本**: Step 3 React 改造（Mode C: Hybrid）  
> **后端版本**: Step 3 API 扩展（copilot_* 字段 + 2 个新增 API）  
> **状态**: ⚠️ 待环境就绪后执行完整联调

---

## 一、前端改造验证

### 1.1 TypeScript 编译

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

| 检查项 | 结果 |
|--------|------|
| TS 错误数 | ✅ 0 errors |

### 1.2 页面改造清单

| 页面 | 文件 | 改造内容 | 状态 |
|------|------|---------|------|
| 看板 | `TaskHubPage.tsx` | 移除 PageHeader「新建任务」按钮 | ✅ |
| 看板 | `TaskHubPage.tsx` | 移除详情抽屉「前往审核/发布确认/预览干预」按钮 | ✅ |
| 看板 | `TaskHubPage.tsx` | 修改 EmptyState 文案（引导 Copilot） | ✅ |
| 创建向导 | `TaskHubCreatePage.tsx` | 移除底部「上一步/下一步/确认创建」按钮 | ✅ |
| 创建向导 | `TaskHubCreatePage.tsx` | 添加 Mode C 提示横幅 | ✅ |
| 编辑器 | `ContentForgePage.tsx` | 移除 PageHeader「新建内容」按钮 | ✅ |
| 编辑器 | `ContentForgePage.tsx` | 移除「AI 生成/重新生成」按钮 | ✅ |
| 编辑器 | `ContentForgePage.tsx` | 移除「保存草稿/提交审核/丢弃」按钮 | ✅ |
| 编辑器 | `ContentForgePage.tsx` | 修改 EmptyState 文案（引导 Copilot） | ✅ |

---

## 二、后端 API 验证

### 2.1 Python 语法检查

```bash
cd apps/backend && python -m py_compile src/api/task_hub.py src/api/copilot.py
```

| 检查项 | 结果 |
|--------|------|
| task_hub.py | ✅ 通过 |
| copilot.py | ✅ 通过 |

### 2.2 路由注册验证（待环境就绪）

| # | 路由 | 方法 | 来源文件 | 状态 |
|---|------|------|---------|------|
| 1 | `/task-hub/tasks` | GET | `task_hub.py` | 🔄 扩展 `copilot_summary` |
| 2 | `/task-hub/tasks/{id}` | GET | `task_hub.py` | 🔄 扩展 `copilot_context` |
| 3 | `/task-hub/tasks` | POST | `task_hub.py` | 🔄 扩展 `copilot_followup` |
| 4 | `/ai/copilot/regenerate-content` | POST | `copilot.py` | ✅ 新增 |
| 5 | `/ai/copilot/save-and-submit` | POST | `copilot.py` | ✅ 新增 |
| 6 | `/ai/copilot/context` | POST | `copilot.py` | ✅ 已有 |
| 7 | `/ai/copilot/action-cards` | GET | `copilot.py` | ✅ 已有 |
| 8 | `/ai/copilot/execute` | POST | `copilot.py` | ✅ 已有 |

### 2.3 API 前缀对齐

| 前端调用 | 后端路由 | 状态 |
|---------|---------|------|
| `GET /api/task-hub/tasks` | `GET /task-hub/tasks` | ✅ 一致 |
| `POST /api/ai/copilot/regenerate-content` | `POST /ai/copilot/regenerate-content` | ✅ 一致 |
| `POST /api/ai/copilot/save-and-submit` | `POST /ai/copilot/save-and-submit` | ✅ 一致 |

---

## 三、联调测试计划（环境就绪后执行）

### 3.1 API 对齐测试

- [ ] `GET /api/task-hub/tasks` — 验证 `copilot_summary.kanban_stats` 计数正确
- [ ] `GET /api/task-hub/tasks/{id}` — 验证 `copilot_context.editor_suggestions` 结构正确
- [ ] `POST /api/task-hub/tasks` — 验证 `copilot_followup.suggested_cards` 包含生成/配置按钮
- [ ] `POST /api/ai/copilot/regenerate-content` — 验证返回 `ACCEPTED` + `copilot_followup`
- [ ] `POST /api/ai/copilot/save-and-submit` — 验证返回 `OK` + `copilot_followup`

### 3.2 Copilot-Driven 端到端测试

- [ ] 看板页：Copilot 展示「新建内容」Action Card → 点击 → 跳转创建向导
- [ ] 创建向导：Copilot 展示「确认创建」Action Card → 点击 → 创建任务 → 工作区同步
- [ ] 编辑器：Copilot 展示「保存草稿」Action Card → 点击 → 保存成功 → 状态更新
- [ ] 编辑器：Copilot 展示「提交审核」Action Card → 点击 → 状态变为 reviewing
- [ ] 编辑器：Copilot 展示「重新生成」Action Card → 点击 → 异步任务 → WebSocket 进度 → 内容更新

### 3.3 边界测试

- [ ] 空数据：看板无任务时 EmptyState 展示正确
- [ ] 超长文本：编辑器输入 5000 字正文，保存正常
- [ ] 网络错误：API 500 时前端展示错误 Card
- [ ] 慢网络：API > 2s 时展示 Loading Skeleton

### 3.4 性能测试

- [ ] 看板首屏 < 2s
- [ ] 任务详情页 < 1s
- [ ] 重新生成提交 < 100ms
- [ ] 保存并提交 < 200ms

---

## 四、后端缺口清单

| # | 文件 | TODO | 实现内容 | 工期 | 阻塞 |
|---|------|------|---------|------|------|
| 1 | `src/api/task_hub.py` | `copilot_summary` 当前为内存计算 | 接入真实任务统计（按 status 分组 COUNT） | 0.5 天 | 否 |
| 2 | `src/api/task_hub.py` | `copilot_context` 当前为静态 mock | 接入真实 AI 建议（调用 LLM Hub 或规则引擎） | 1 天 | 否 |
| 3 | `src/api/copilot.py` | `regenerate-content` 为 stub | Celery 异步队列 + ContentForge Agent 调用 | 2 天 | Sprint 2 |
| 4 | `src/api/copilot.py` | `save-and-submit` 为 stub | 原子操作：保存草稿 + 状态流转 + 审计日志 | 1 天 | Sprint 2 |
| 5 | `src/models/task_orm.py` | 新字段未添加 | `copilot_suggested_agent` / `copilot_priority_score` / `copilot_context_json` | 0.5 天 | Sprint 1 |
| 6 | Alembic | 无迁移脚本 | `content_regeneration_jobs` 表 + `tasks` 扩展字段 | 0.5 天 | Sprint 1 |
| 7 | WebSocket | 无内容生产事件 | `content.generation.progress` / `content.regeneration.complete` | 1 天 | Sprint 2 |

---

## 五、结论

| 检查项 | 结果 |
|--------|------|
| 前端编译 | ✅ 0 errors |
| 后端语法 | ✅ 通过 |
| API 路径对齐 | ✅ 一致 |
| 响应格式对齐 | ✅ 统一 `_base_response` + `copilot_*` |
| 环境就绪 | ❌ 需启动 PostgreSQL + Redis |
| 后端实现完成度 | ~40%（接口定义完成，业务逻辑 stub） |

**建议**:
1. 启动 PostgreSQL + Redis 后立即执行路由注册验证
2. 后端优先完成 Sprint 1（模型迁移 + `copilot_summary` 真实计算）
3. 前端保持 `USE_MOCK = true`，后端每完成一个 API 立即验证
4. 编辑器页面 `ContentForgePage.tsx` 需进一步改造 Inline AI 和六层 Prompt 可视化

---

*报告生成: 2026-06-05*  
*测试人: Kimi Code CLI*
