# 数据库模型层总览

> 自动生成于 2026-05-27

---

## 一、SQLAlchemy ORM 模型文件（14 个）

### 1. `asset_pool_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `AssetORM` |
| **表名** | `assets` |
| **主键** | `id` (UUID, pk) |
| **索引列** | `series_id`, `brand_knowledge_id`, `tenant_id` |
| **复合索引** | `ix_assets_tenant_status` (tenant_id, status)<br>`ix_assets_source_category` (source_type, category) |
| **主要字段** | `filename`(String255), `file_url`(Text), `thumbnail_url`(Text), `source_type`(String20), `license_type`(String20), `license_status`(String20), `copyright_holder`(String255), `copyright_year`(Integer), `usage_rights`(JSON), `copyright_validated`(Boolean), `license_ref`(Text), `stock_source`(String50), `stock_id`(String100), `license_expiry`(DateTime), `ai_model`(String100), `ai_prompt`(Text), `ai_disclosure`(Boolean), `ai_metadata`(JSON), `category`(String50), `tags`(JSON), `description`(Text), `status`(String20), `meta_width`(Integer), `meta_height`(Integer), `meta_file_size`(Integer), `meta_mime_type`(String50), `meta_dominant_color`(String20), `uploaded_by`(String100), `tenant_id`(String64), `created_at`, `updated_at` |
| **外键/关系** | 无外键；`brand_knowledge_id` 为逻辑关联 |

---

### 2. `brand_knowledge_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `BrandKnowledgeEntryORM` |
| **表名** | `brand_knowledge_entries` |
| **主键** | `id` (UUID, pk) |
| **索引列** | `product_id`, `approval_number`, `brand_name`, `tenant_id` |
| **复合索引** | `ix_bk_tenant_type` (tenant_id, entry_type)<br>`ix_bk_brand_latest` (brand_name, is_latest) |
| **主要字段** | `entry_type`(String50), `name`(String255), `content`(Text), `product_id`(String32), `approval_number`(String50), `sku_code`(String100), `brand_name`(String100), `prohibited_claims`(JSON), `required_disclaimers`(JSON), `embedding`(Vector(1536)), `version`(Integer), `is_latest`(Boolean), `parent_id`(UUID), `asset_ids`(JSON), `created_by`(String100), `updated_by`(String100), `change_reason`(Text), `tenant_id`(String64), `created_at`, `updated_at` |
| **外键** | `parent_id` → `brand_knowledge_entries.id` (ON DELETE SET NULL) |
| **关系** | 无 `relationship` 定义 |

---

### 3. `content_draft_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `ContentDraftORM` |
| **表名** | `content_drafts` |
| **主键** | `id` (String64, pk) |
| **索引列** | `account_id`, `created_by` |
| **复合索引** | `ix_cd_account` (account_id)<br>`ix_cd_created_by` (created_by) |
| **主要字段** | `title`(String255), `content_type`(String16), `platform`(String16), `account_id`(String64), `body`(Text), `tags`(JSON), `status`(String16), `cover_image_url`(String512), `engagement_estimate`(Float), **`created_by`(String64, nullable, index)**, `created_at`, `updated_at`, `published_at` |
| **外键/关系** | 无外键；`account_id` 为逻辑关联 |

---

### 4. `llm_hub_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `LLMModelORM` |
| **表名** | `llm_models` |
| **主键** | `id` (UUID, pk) |
| **主要字段** | `provider`(String64), `model_name`(String128), `api_key_encrypted`(Text), `endpoint_base_url`(String512), `status`(String16), `data_training_opt_out`(Boolean), `created_at`, `updated_at` |
| **外键/关系** | 无 |

| 项目 | 内容 |
|------|------|
| **ORM 类** | `LLMScopeConfigORM` |
| **表名** | `llm_scope_configs` |
| **主键** | `id` (UUID, pk) |
| **主要字段** | `scope_type`(String16), `node_id`(String64), `model_id`(UUID), `temperature`(Float), `timeout_seconds`(Integer), `created_at`, `updated_at` |
| **外键** | `model_id` → `llm_models.id` (ON DELETE CASCADE) |
| **约束/索引** | `CheckConstraint`: scope_type ∈ {global, node}<br>`uq_global_scope` (scope_type, unique)<br>`uq_node_scope` (node_id, unique) |

| 项目 | 内容 |
|------|------|
| **ORM 类** | `LLMUsageLogORM` |
| **表名** | `llm_usage_logs` |
| **主键** | `id` (UUID, pk) |
| **主要字段** | `model_id`(UUID), `node_id`(String64), `provider_region`(String16), `input_tokens`(Integer), `output_tokens`(Integer), `latency_ms`(Integer), `status`(String16), `error_message`(Text), `created_at` |
| **外键** | `model_id` → `llm_models.id` (ON DELETE SET NULL) |

| 项目 | 内容 |
|------|------|
| **ORM 类** | `LLMPricingORM` |
| **表名** | `llm_pricing` |
| **主键** | `model_name` (String128, pk) |
| **主要字段** | `provider`(String64), `input_price_per_1k`(Numeric(10,6)), `output_price_per_1k`(Numeric(10,6)), `currency`(String8) |
| **外键/关系** | 无 |

---

### 5. `orm_user.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `UserORM` |
| **表名** | `users` |
| **主键** | `id` (UUID, pk) |
| **索引列** | `email` (unique), `tenant_id` |
| **主要字段** | `email`(String255, unique), `username`(String100), `hashed_password`(String255), `role`(String50), `is_active`(Boolean), `mfa_secret`(String255), `mfa_enabled`(Boolean), `tenant_id`(String64), `created_at`, `updated_at` |
| **外键/关系** | 无 |

---

### 6. `persona_story_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `PersonaStoryORM` |
| **表名** | `persona_stories` |
| **主键** | `id` (UUID, pk) |
| **索引列** | `persona_id` |
| **复合索引** | `ix_ps_persona_status` (persona_id, status) |
| **主要字段** | `persona_id`(String64), `name`(String256), `description`(Text), `emotion_curve_template`(String32), `status`(String16), `created_at`, `updated_at` |
| **关系** | `nodes` → `StoryNodeORM` (back_populates="story", cascade="all, delete-orphan", order_by="sequence_index") |

| 项目 | 内容 |
|------|------|
| **ORM 类** | `StoryNodeORM` |
| **表名** | `story_nodes` |
| **主键** | `id` (UUID, pk) |
| **索引列** | `content_draft_id` |
| **复合索引** | `ix_sn_story_seq` (story_id, sequence_index) |
| **唯一约束** | `uq_story_node_seq` (story_id, sequence_index) |
| **主要字段** | `story_id`(UUID), `sequence_index`(Integer), `theme`(String256), `emotion_tone`(String16), `key_event`(Text), `prev_recap`(Text), `next_teaser`(Text), `content_draft_id`(String64), `created_at`, `updated_at` |
| **外键** | `story_id` → `persona_stories.id` (ON DELETE CASCADE) |
| **关系** | `story` → `PersonaStoryORM` (back_populates="nodes") |

---

### 7. `platform_rule_attribution_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `ContentRuleAttributionORM` |
| **表名** | `content_rule_attributions` |
| **主键** | `id` (UUID, pk) |
| **索引列** | `content_id`, `tenant_id` |
| **复合索引** | `idx_attr_content_id` (content_id, evaluated_at)<br>`idx_attr_rule_id` (rule_id, evaluated_at) |
| **主要字段** | `content_id`(String255), `rule_id`(String255), `rule_name`(String255), `layer`(String50), `action`(String20), `matched_text`(Text), `platform`(String20), `evaluated_at`(DateTime), `tenant_id`(String64) |
| **外键/关系** | 无 |

---

### 8. `platform_rule_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `PlatformRuleORM` |
| **表名** | `platform_rules` |
| **主键** | `id` (UUID, pk) |
| **索引列** | `tenant_id` |
| **复合索引** | `ix_pr_platform_layer` (platform, layer)<br>`ix_pr_platform_enabled` (platform, enabled) |
| **主要字段** | `platform`(String20), `layer`(String10), `name`(String255), `description`(Text), `condition_json`(JSON), `action`(String20), `priority`(Integer), `enabled`(Boolean), `version`(Integer), `effective_from`(DateTime), `effective_until`(DateTime), `applicable_lifecycle`(JSON), `created_by`(String100), `updated_by`(String100), `tenant_id`(String64), `created_at`, `updated_at` |
| **外键/关系** | 无 |

| 项目 | 内容 |
|------|------|
| **ORM 类** | `PlatformRuleHistoryORM` |
| **表名** | `platform_rule_history` |
| **主键** | `id` (UUID, pk) |
| **索引列** | `rule_id` |
| **复合索引** | `ix_prh_rule_version` (rule_id, version) |
| **主要字段** | `rule_id`(UUID), `platform`(String20), `layer`(String10), `name`(String255), `condition_json`(JSON), `action`(String20), `priority`(Integer), `enabled`(Boolean), `version`(Integer), `effective_from`(DateTime), `change_reason`(Text), `changed_by`(String100), `created_at` |
| **外键** | `rule_id` → `platform_rules.id` (ON DELETE CASCADE) |

---

### 9. `prohibited_word_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `ProhibitedWordORM` |
| **表名** | `prohibited_words` |
| **主键** | `id` (UUID, pk) |
| **主要字段** | `word`(String255), `category`(String50), `severity`(String10), `platform`(String20), `match_type`(String20), `is_enabled`(Boolean), `description`(String500), `tenant_id`(String64), `created_by`(String255), `updated_by`(String255), `created_at`, `updated_at` |
| **复合索引** | `idx_prohibited_word_platform` (platform, is_enabled)<br>`idx_prohibited_word_tenant` (tenant_id, is_enabled)<br>`idx_prohibited_word_category` (category, is_enabled) |
| **外键/关系** | 无 |

| 项目 | 内容 |
|------|------|
| **ORM 类** | `ContentGuidelineORM` |
| **表名** | `content_guidelines` |
| **主键** | `id` (UUID, pk) |
| **主要字段** | `name`(String255), `category`(String50), `description`(String1000), `rules_json`(String2000), `platform`(String20), `is_enabled`(Boolean), `tenant_id`(String64), `created_by`(String255), `updated_by`(String255), `created_at`, `updated_at` |
| **复合索引** | `idx_guideline_platform` (platform, is_enabled)<br>`idx_guideline_category` (category, is_enabled) |
| **外键/关系** | 无 |

---

### 10. `publish_task_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `PublishTaskORM` |
| **表名** | `publish_tasks` |
| **主键** | `id` (String64, pk) |
| **索引列** | `draft_id`, `account_id`, `task_hub_task_id`, `created_by` |
| **复合索引** | `ix_pt_draft` (draft_id)<br>`ix_pt_account` (account_id)<br>`ix_pt_task_hub` (task_hub_task_id)<br>`ix_pt_created_by` (created_by) |
| **主要字段** | `draft_id`(String64), `account_id`(String64), `platform`(String16), `status`(String16), `scheduled_at`(DateTime), `published_at`(DateTime), `published_url`(String512), `platform_post_id`(String128), `error_reason`(String512), `publish_skipped_reason`(String512), `retry_count`(Integer), `task_hub_task_id`(String64), **`created_by`(String64, nullable, index)**, `created_at`, `updated_at` |
| **外键/关系** | 无 |

---

### 11. `review_record_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `ReviewRecordORM` |
| **表名** | `review_records` |
| **主键** | `id` (String64, pk) |
| **索引列** | `task_id`, `task_created_by` |
| **复合索引** | `ix_rr_task` (task_id)<br>`ix_rr_task_created_by` (task_created_by) |
| **主要字段** | `task_id`(String64), `reviewer`(String64), `decision`(String16), `reason`(String512), `target_node_index`(Integer), `revised_variables`(JSON), `publish_mode`(String16), `scheduled_at`(DateTime), `is_dual_approval`(Boolean), `dual_approver`(String64), **`task_created_by`(String64, nullable, index)**, `created_at` |
| **外键/关系** | 无 |

---

### 12. `task_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `TaskORM` |
| **表名** | `tasks` |
| **主键** | `id` (UUID, pk) |
| **索引列** | `account_id`, `persona_id`, `persona_story_id`, `draft_id`, `tenant_id` |
| **复合索引** | `ix_tasks_status` (status)<br>`ix_tasks_account` (account_id)<br>`ix_tasks_persona` (persona_id)<br>`ix_tasks_template` (workflow_template_id)<br>`ix_tasks_parent` (parent_task_id)<br>`ix_tasks_tenant_status` (tenant_id, status) |
| **主要字段** | `name`(String255), `workflow_template_id`(String64), `workflow_version`(Integer), `account_id`(String64), `persona_id`(String64), `persona_story_id`(String64), `node_id`(String64), `content_series_id`(String64), `platform`(String16), `status`(String32), `priority`(Integer), `current_node_index`(Integer), `prompt_variables`(JSON), `draft_id`(String64), `parent_task_id`(String64), `scheduled_at`(DateTime), `created_by`(String64), `review_decision`(String16), `reviewed_at`(DateTime), `reviewer`(String64), `review_reason`(String512), `publish_confirmed_at`(DateTime), `publish_confirmer`(String64), `cron_job_id`(String64), `trace_id`(String32), `execution_id`(String64), `tenant_id`(String64), `created_at`, `updated_at`, `completed_at` |
| **外键/关系** | 无 |

---

### 13. `timeline_library_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `TimelineEventORM` |
| **表名** | `timeline_events` |
| **主键** | `id` (UUID, pk) |
| **索引列** | `tenant_id` |
| **复合索引** | `ix_te_tenant_dates` (tenant_id, start_date, end_date)<br>`ix_te_type_dates` (event_type, start_date) |
| **主要字段** | `name`(String255), `event_type`(String50), `description`(Text), `start_date`(DateTime), `end_date`(DateTime), `recurring`(Boolean), `cron_expression`(String100), `cron_job_id`(String64), `year`(Integer), `brand_knowledge_ids`(JSON), `product_ids`(JSON), `prohibited_claims`(JSON), `is_commercial`(Boolean), `status`(String20), `priority`(Integer), `color_code`(String20), `created_by`(String100), `tenant_id`(String64), `created_at`, `updated_at` |
| **外键/关系** | 无 |

---

### 14. `note_engagement_orm.py` ⭐ 新增

| 项目 | 内容 |
|------|------|
| **ORM 类** | `NoteEngagementORM` |
| **表名** | `note_engagements` |
| **主键** | `id` (String64, pk) |
| **索引列** | `account_id`, `platform_post_id`, `fetch_status` |
| **复合索引** | `ix_note_engagements_publish_task_id` (publish_task_id)<br>`ix_note_engagements_platform_post_id` (platform_post_id)<br>`ix_note_engagements_account_id` (account_id) |
| **主要字段** | `publish_task_id`(String64), `account_id`(String64), `platform_post_id`(String128), `likes`(Integer), `comments`(Integer), `saves`(Integer), `shares`(Integer), `views`(Integer), `fetch_status`(String16, default="pending"), `fetch_error`(String512), `fetched_at`(DateTime), `raw_response`(JSON), `created_at`, `updated_at` |
| **外键** | `publish_task_id` → `publish_tasks.id` (ON DELETE CASCADE) |
| **关系** | `publish_task` → `PublishTaskORM` (back_populates="engagement") |

---

### 15. `vet_drug_orm.py`

| 项目 | 内容 |
|------|------|
| **ORM 类** | `VetDrugEntryORM` |
| **表名** | `vet_drug_entries` |
| **主键** | `id` (UUID, pk) |
| **索引列** | `approval_number` (unique), `product_name`, `brand_knowledge_id`, `tenant_id` |
| **复合索引** | `ix_vd_tenant_status` (tenant_id, status)<br>`ix_vd_expiry` (expiry_date, status) |
| **主要字段** | `approval_number`(String50, unique), `product_name`(String255), `generic_name`(String255), `english_name`(String255), `manufacturer`(String255), `manufacturer_address`(Text), `ingredients`(Text), `specifications`(Text), `indications`(Text), `usage_dosage`(Text), `contraindications`(Text), `adverse_reactions`(Text), `precautions`(Text), `drug_interactions`(Text), `storage_conditions`(Text), `category`(String50), `drug_type`(String50), `issue_date`(DateTime), `expiry_date`(DateTime), `status`(String20), `applicable_species`(JSON), `target_diseases`(JSON), `tags`(JSON), `brand_knowledge_id`(String32), `created_by`(String100), `updated_by`(String100), `data_source`(String50), `tenant_id`(String64), `created_at`, `updated_at` |
| **外键/关系** | 无 |

---

## 二、非 ORM 模型文件（Dataclass / 内存存储）

| 文件名 | 类名 | 说明 |
|--------|------|------|
| `account_pool.py` | `FingerprintProfile` | 账号指纹配置 dataclass |
| | `ProxyConfig` | 代理配置 dataclass |
| | `AccountPoolEntry` | 账号池条目 dataclass（内存存储） |
| `content_draft.py` | `ContentDraft` | 内容草稿 dataclass（内存存储） |
| `dashboard.py` | `TodayOverview` | 仪表盘今日概览 dataclass |
| | `QuickAction` | 快捷操作 dataclass |
| | `Alert` | 告警 dataclass |
| | `ActivityEntry` | 活动日志 dataclass |
| `persona.py` | `Persona` | 人设声音 dataclass（内存存储） |
| `platform_account.py` | `PlatformAccount` | 平台账号 dataclass（内存存储） |
| `proxy_config.py` | `ProxyConfigEntry` | 代理服务器配置 dataclass（内存存储） |
| `publish_task.py` | `PublishTask` | 发布任务 dataclass（内存存储） |
| `user.py` | `User` | 用户普通类（`UserORM` 包装兼容层） |

---

## 三、汇总统计

| 类别 | 数量 |
|------|------|
| ORM 模型文件 | **14** 个 |
| 非 ORM 模型文件 | **8** 个 |
| **ORM 表总数** | **21** 张表 |
| **ORM 类总数** | **21** 个 |

### 21 张 ORM 表清单

1. `assets`
2. `brand_knowledge_entries`
3. `content_drafts`
4. `llm_models`
5. `llm_scope_configs`
6. `llm_usage_logs`
7. `llm_pricing`
8. `users`
9. `persona_stories`
10. `story_nodes`
11. `content_rule_attributions`
12. `platform_rules`
13. `platform_rule_history`
14. `prohibited_words`
15. `content_guidelines`
16. `publish_tasks`
17. `review_records`
18. `tasks`
19. `timeline_events`
20. `vet_drug_entries`

### 模型间关系总览

| 父表 | 子表 | 外键 | 级联策略 |
|------|------|------|----------|
| `persona_stories` | `story_nodes` | `story_id` | CASCADE |
| `llm_models` | `llm_scope_configs` | `model_id` | CASCADE |
| `llm_models` | `llm_usage_logs` | `model_id` | SET NULL |
| `platform_rules` | `platform_rule_history` | `rule_id` | CASCADE |
| `brand_knowledge_entries` | `brand_knowledge_entries` | `parent_id` | SET NULL |
