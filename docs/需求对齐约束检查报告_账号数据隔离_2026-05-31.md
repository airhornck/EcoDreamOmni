# 需求对齐约束 — 六章强制检查执行报告

> **对应需求**：账号数据隔离改造（公共数据共享 + 按创建者隔离任务与审核）
> 
> **执行日期**：2026-05-31
> 
> **执行结果**：🔴 **检查未通过 — 触发架构调整红线 + PRD 缺失**
> 
> **结论**：必须先补充 PRD、更新偏差报告，并经用户审核架构调整方案后，方可进入实施阶段。

---

## 第一章：PRD 业务真源对齐（强制）

### 1.1 该需求是否存在于 PRD 中？

**结论：❌ 不存在。属于新增/偏差需求。**

| 检查项 | PRD 状态 | 用户实际需求 | 判定 |
|--------|---------|-------------|------|
| 按创建者隔离任务数据 | PRD 全文**未定义** | "每个账号只能看到自己发布的任务" | **偏差** |
| 审核发布中心只能查看自己的任务 | PRD 全文**未定义** | "每个账号只能看到自己的任务审核" | **偏差** |
| `created_by` 字段用途 | 仅作为**审计字段**存在于数据模型（§3.3、§8.2、§8.10 等） | 需作为**数据隔离过滤条件** | **偏差** |
| `tenant_id` 字段用途 | 存在于多张表 ORM 定义，但无查询隔离需求 | 实际完全未使用 | **偏差** |
| "数据隔离"/"多租户"/"用户隔离" | PRD **全文未出现** | — | **偏差** |

**PRD 中已有的相关内容**：
- §7.2.1 AgentHub 的"环境隔离"（dev/staging/prod）和 RBAC —— 仅针对 **Agent 配置管理**，非 TaskHub 数据隔离。
- §10.6 Human-in-the-Loop —— 仅定义审核操作类型和状态流转，**未规定任务归属或审核权限范围**。

### 1.2 需求范围是否超出当前 Phase？

| 评估项 | 结论 |
|--------|------|
| 当前 Phase | Phase 1 后期（接近 Phase 1.5，TASK_V2.7.1 基线 W1-W22 已完成） |
| 需求归属 | 属于 **Phase 1 基础能力补全**（权限与数据隔离是 SaaS 基础），但 PRD 中未明确规划 |
| 是否超出 | 🟡 **不超出技术边界，但超出 PRD 定义范围** —— 属于 PRD 遗漏的基础需求 |

### 1.3 强制行动

按照约束 §1.1：
> "如果 PRD 中未定义或与 PRD 存在偏差，必须在实施前，将需求描述补充到 `EcoDream_Omni_PRD_v2_对齐核心方案.md` 的对应章节，同步更新 `docs/PRD偏差报告.md`"

**待办**：
- [ ] 在 PRD 中补充 "§x.x 数据隔离与账号权限" 章节，明确：
  - 【账号与人设】【基础功能】【系统治理】【系统设置】为公共数据
  - 【内容生产→任务中心】按 `created_by` 隔离
  - 【风控与发布→审核发布中心】按 `created_by` 隔离
- [ ] 更新 `docs/PRD偏差报告.md`，新增偏差项并标记状态为 🟡 待修复

---

## 第二章：技术架构对齐（强制）

### 2.1 调用关系维护原则评估

| 改造内容 | 是否允许 | 说明 |
|----------|---------|------|
| API 层引入 `get_current_user` | ✅ 允许 | 在已有 Router 下修改端点函数签名，属于维护调用关系 |
| Service 层增加 `created_by` 过滤参数 | ✅ 允许 | 在已有 Function 文件中新增参数和查询条件 |
| 前端 Store 移除硬编码 `operator` | ✅ 允许 | 不涉及全局状态结构变更 |
| 为 `TaskORM` 的 `created_by` 增加查询过滤 | ✅ 允许 | 不涉及新增 Service/Function 文件 |

### 2.2 架构调整红线评估

**结论：❌ 触发红线 —— 必须暂停实施，请求用户严格审核**

| 架构调整类型 | 是否触发 | 具体说明 |
|-------------|---------|---------|
| **修改数据库字段** | 🔴 **触发** | 需为 `PublishTaskORM`、`ContentDraftORM`、`ReviewRecordORM` 新增 `created_by` 字段，需要 **Alembic 迁移** |
| 新增 API Router | ❌ 否 | 无新增路由文件 |
| 新增 Service 文件 | ❌ 否 | 无新增 Service 文件 |
| 新增 ORM 模型 | ❌ 否 | 无新增模型文件 |
| 修改路由前缀 | ❌ 否 | 无 |
| 引入新的外部依赖 | ❌ 否 | 无 |
| 修改 Celery 任务签名 | ❌ 否 | 无 |
| 修改前端路由结构 | ❌ 否 | 无 |
| 修改全局状态设计 | ❌ 否 | 无 |

### 2.3 架构影响范围评估

如果实施数据隔离改造，影响的文件和调用链如下：

```
【数据库层】
├── publish_tasks 表          ← 新增 created_by 列（Alembic 迁移）
├── content_drafts 表         ← 新增 created_by 列（Alembic 迁移）
├── review_records 表         ← 新增 task_created_by 列（Alembic 迁移）

【后端 API 层】
├── api/task_hub.py           ← 20+ 个端点引入 get_current_user
├── api/human_in_loop.py      ← 7 个端点引入 get_current_user
├── api/review_publish.py     ← 5 个端点引入 get_current_user
├── api/publisher.py          ← 6 个端点引入 created_by 过滤

【后端 Service 层】
├── services/task_hub.py      ← list_tasks 增加 created_by 参数
├── services/task_function.py ← list_tasks_from_db 增加 created_by 过滤
├── services/human_in_loop.py ← pending 列表/审核操作加隔离校验
├── services/publisher_service.py ← list_publish_tasks 增加 created_by 过滤

【前端层】
├── stores/taskHubStore.ts           ← 移除 created_by 硬编码
├── stores/reviewPublishStore.ts     ← 移除 operator 硬编码
├── pages/TaskHubCreatePage.tsx      ← 移除 created_by 硬编码

【缓存层】
├── services/task_hub.py _task_db    ← 内存缓存需评估是否禁用或分片
```

### 2.4 强制行动

按照约束 §2.2：
> "如果需求涉及架构调整，必须立即停止实施，向用户说明具体影响范围，等待用户明确批准后再继续。"

**待办**：
- [ ] 向用户提交架构影响评估（本报告 §2.3 已完成）
- [ ] 等待用户明确批准 "为 PublishTaskORM / ContentDraftORM / ReviewRecordORM 新增 `created_by` 字段并执行 Alembic 迁移"
- [ ] 如用户批准，继续执行后续章节

---

## 第三章：文档连锁更新（强制）

### 3.1 需要更新的文档清单

如果实施本需求，必须同步更新以下文档：

| 修改类型 | 必须更新的文档 | 当前状态 |
|----------|---------------|---------|
| 新增 ORM 字段 | `数据词典/01-数据库模型层.md` | 已有基础记录，需补充 `created_by` 字段 |
| 修改 API 端点（引入认证依赖） | `数据词典/02-后端API路由层.md` | 当前未记录认证依赖，需补充 |
| 修改 Service 函数签名 | `数据词典/03-后端Service层.md` | `task_hub.py`/`human_in_loop.py` 签名当前**空白**，需补全 |
| 修改前端 Store | `数据词典/04-前端Store与路由.md` | Store 名称列大面积空白，需补全 |
| 修改调用关系 | `数据词典/05-模块调用关系矩阵.md` | 目标模块调用链路当前**空白**，需补全 |
| 新增 PRD 章节 | `EcoDream_Omni_PRD_v2_对齐核心方案.md` | 需新增数据隔离章节 |
| 更新偏差报告 | `docs/PRD偏差报告.md` | 需新增偏差项 |
| 新增分析文档 | `docs/文档总纲.md` | 需登记 `账号数据隔离改造分析报告` |

### 3.2 文档孤岛检查

| 检查项 | 状态 |
|--------|------|
| 是否存在不在 `文档总纲.md` 中登记的文档？ | 🟡 `docs/账号数据隔离改造分析报告_任务与审核发布中心.md` 尚未登记 |
| 是否存在与总纲描述不一致的文档内容？ | 🟡 `数据词典/03-后端Service层.md` 中 `task_hub.py`、`human_in_loop.py` 函数签名空白，与代码实际不符 |

### 3.3 强制行动

- [ ] 实施前确认 `文档总纲.md` 已登记所有相关文档
- [ ] 实施中同步更新 01-05 数据词典分册
- [ ] 实施后验证文档与代码一致性

---

## 第四章：变更记录登记（强制）

### 4.1 变更记录路径

```
docs/变更记录/2026-05-31/开发变更总结_2026-05-31.md
```

### 4.2 变更记录需包含的内容（待实施时填写）

| 章节 | 预计内容 |
|------|---------|
| 需求概述 | 公共数据共享 + 按创建者隔离任务与审核发布中心 |
| PRD 对齐说明 | 新增需求，补充至 PRD §x.x |
| 修改文件清单 | api/task_hub.py、api/human_in_loop.py、api/review_publish.py、api/publisher.py、services/task_hub.py、services/task_function.py、services/human_in_loop.py、services/publisher_service.py、models/publish_task_orm.py、models/content_draft_orm.py、models/review_record_orm.py、stores/taskHubStore.ts、stores/reviewPublishStore.ts、pages/TaskHubCreatePage.tsx |
| 调用关系变更 | API 层引入 `get_current_user` → Service 层增加 `created_by` 过滤 → ORM 层新增字段 |
| 前端映射变更 | Store 移除 `created_by`/`operator` 硬编码，由后端 JWT 自动注入 |
| 测试验证 | pytest 隔离测试用例 |
| 文档更新 | 数据词典 01-05 + PRD + 偏差报告 + 总纲 |
| 风险与后续 | 内存缓存 `_task_db` 需评估；Alembic 迁移回滚方案 |

### 4.3 强制行动

- [ ] 实施完成后按模板填写变更记录

---

## 第五章：工程纪律约束（强制）

### 5.1 Red-Green-Blue 循环

| 阶段 | 计划行动 |
|------|---------|
| 🔴 Red | 编写测试验证 "用户 A 无法查看/操作用户 B 的任务" —— 当前应失败 |
| 🟢 Green | 最小化修改 API 层注入 `get_current_user` 并增加 `created_by` 过滤 |
| 🔵 Blue | 重构 Service 层查询逻辑，提取通用的 `ownership_check` 函数 |

### 5.2 质量门禁

| 门禁 | 阈值 | 预计影响 |
|------|------|---------|
| pytest 覆盖率 | ≥ 80% | 需新增隔离场景测试用例 |
| mypy | 0 错误 | `get_current_user` 注入后类型推导需验证 |
| 前端 vitest | 全绿 | 如有前端逻辑变更需补充 |
| tsc --noEmit | 0 错误 | 移除硬编码后类型检查 |

### 5.3 禁止事项检查

| 禁止项 | 当前计划是否违反 |
|--------|-----------------|
| 不写测试直接实现功能 | ✅ 不违反 —— 计划先写 Red 测试 |
| 跳过红灯阶段 | ✅ 不违反 |
| 一次修改多个不相关模块 | ⚠️ **注意** —— 本需求涉及 api + services + models + frontend，需按 Phase 分批实施，避免一次全改 |
| 删除或修改他人写的测试 | ✅ 不违反 |
| 修改后不更新数据词典和总纲 | ⚠️ **注意** —— 文档更新量大，需专人跟进 |

---

## 第六章：快速检查清单（执行前默念）

```
□ 该需求在 PRD 中有定义吗？
  → 没有 → 需补充到 PRD + 更新偏差报告 ✅ 已识别

□ 修改仅维护调用关系吗？
  → 涉及架构调整（修改数据库字段）→ 触发红线 ✅ 已识别

□ 涉及哪些文件？
  → 已列出 15+ 个文件清单 ✅ 已识别

□ 涉及哪些文档？
  → 数据词典 01-05 + PRD + 偏差报告 + 总纲 ✅ 已识别

□ 测试策略是什么？
  → 先写复现测试（Red-Green-Blue）✅ 已识别

□ 变更记录格式准备好了吗？
  → 已按模板准备 ✅ 已识别
```

---

## 综合结论与下一步行动

| 检查章节 | 状态 | 关键发现 |
|----------|------|---------|
| 第一章 PRD 对齐 | 🔴 **未通过** | 需求在 PRD 中**未定义**，属新增/偏差需求 |
| 第二章 架构对齐 | 🔴 **未通过** | **触发架构调整红线**（修改数据库字段 + Alembic 迁移） |
| 第三章 文档连锁 | 🟡 待执行 | 数据词典 03/04/05 当前存在大量空白，需补全 |
| 第四章 变更记录 | 🟢 已准备 | 模板和路径已就绪 |
| 第五章 工程纪律 | 🟢 符合 | 计划遵循 Red-Green-Blue，但需注意分批实施 |
| 第六章 快速清单 | 🟢 已回答 | 六项均已回答 |

### 🔴 必须先完成的阻断项

1. **补充 PRD**：在 `EcoDream_Omni_PRD_v2_对齐核心方案.md` 中新增数据隔离章节
2. **更新偏差报告**：在 `docs/PRD偏差报告.md` 中登记本需求为新增偏差
3. **用户架构审核**：确认是否批准 "为 PublishTaskORM / ContentDraftORM / ReviewRecordORM 新增 `created_by` 字段并执行 Alembic 迁移"

### 🟢 阻断项解除后可进入的实施路径

- **Phase 1**（优先）：不改动数据库，仅对已有 `created_by` 字段的 `TaskORM` 做 API 层隔离（注入 `get_current_user` + 查询过滤）。
- **Phase 2**（需用户批准 Alembic 迁移后）：补全 `PublishTaskORM`、`ContentDraftORM`、`ReviewRecordORM` 的 `created_by` 字段，实现全链路隔离。

---

*检查执行人: Kimi Code CLI | 日期: 2026-05-31 | 依据: docs/需求对齐约束_通用提示词.md v1.0*
